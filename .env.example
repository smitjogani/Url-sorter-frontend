# Example environment variables for frontend (Vite)
# Use VITE_ prefix for Vite to expose to client-side code
VITE_API_URL=https://url-sorter-backend.onrender.com

# Optional: override for local dev
# VITE_API_URL=http://localhost:5000
